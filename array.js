// Simple array
const arraycities = ['Stockholm', 'Paris', 'Madrid', 'Rome'];
//console.log(arraycities[2]);
// Will return Madrid

// Or simply Print all values of the array
for(let a in arraycities) {
    console.log(a); // Print the index
  console.log(arraycities[a]);
}
